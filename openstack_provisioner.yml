---
- name: Test
  hosts: all
#  become: true
#  connection: ssh
  gather_facts: false

  tasks:
    - raw: hostname
      register: test

    - debug: 
        var: test


#  roles:
#    - lab_openstack
